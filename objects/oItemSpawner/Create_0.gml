// spawns item quick for first time
first_time = irandom_range(60, 100);

// time for rest of game
time = irandom_range(200, 400);

// timer
timer = first_time;

// max spawns
spawns = 0;
spawnMax = 20;
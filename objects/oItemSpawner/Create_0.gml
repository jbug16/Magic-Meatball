// spawns item quick for first time
first_time = irandom_range(40, 60);

// time for rest of game
time = choose(irandom_range(100, 300), irandom_range(700,800));

// timer
timer = first_time;

// max spawns
spawns = 0;
spawnMax = 60;